import { ThemeConfig } from '../../../__test-system__/test-utils/types';

export const unstableLayoutThemeConfig: ThemeConfig = {
  rootTestId: 'unstable-container',
  themeClassPrefix: 'theme-',
  defaultTheme: 'light',
  availableThemes: ['light', 'dark'],
};
